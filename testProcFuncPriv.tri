let
    private
        func doble(var i : Integer) : Integer ~
            triplicar(i)/3*2
	end
    in
        proc triplicar(var i : Integer) ~
            i := i + doble(i)
        end
    end
in
    skip
end