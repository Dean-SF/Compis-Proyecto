let
	rec
		func doble(var i : Integer) : Integer ~
			triplicar(i) / 3*2
		|
		func triplicar(var i : Integer) : Integer ~
			i + doble(i)
		|
		func duplicar(var i : Integer) : Integer ~
			doble(i)
	end
in
	skip
end