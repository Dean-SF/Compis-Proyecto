! Packages: Caso Error
package myPackage ~
    var myVar : Integer;
    proc unProc (var hola : Integer) ~
        myVar := 5
    end
end
let
    var aVar : Integer
in
    myPackage $ unProc (var aVar);
    myPackage2 $ unProc (var aVar)
end

